# RC (remote control) feature — status

Working branch: `code-rc` (shared with a second agent; never push). Spec (source of truth, kept current): `docs/plans/rc-remote-control-spec.md`.

## Done (as of 2026-09-06)
- **rc extension** `pi-extensions/rc/index.ts` (~950 lines): stdlib-only RFC6455 WS server on `node:http`, fixed port 47800, `/rc` toggle command, hello auth with 6-digit decimal code, per-IP rate limit (lock at >=5 failures, 60 s), state/history builders, event forwarding (8 events), prompt/steer/abort via `pi.sendUserMessage`/`ctx.abort`, streaming buffer for mid-stream connects, session rebind on `new_session`, 90 s stale close, port-busy detection, quit auth file.
- **Remote ask** (Task 2): singleton `ask({kind, params, signal?}) -> Promise<answer|null>` (signal = TUI esc-hatch abort, internal API, not wire); `pi-extensions/question/index.ts` + `questionnaire/index.ts` check `globalThis[Symbol.for("pi-rc")]` before the TUI path; server-generated ids (8 hex). TUI mode routes through a non-blocking wait panel (`ctx.ui.custom()`, "Question sent to remote client(s). / Press Esc to answer locally."): Esc aborts the signal → `cancelPendingAsk` broadcasts `question_resolved by:cancelled` + resolves `null` → the tool falls through to the local TUI prompt. ANY `null` (Esc / toggle-off / all clients disconnected) falls through in TUI mode; non-TUI `null` reports cancelled. `cancelPendingAsk(state)` is the single cancel path (stop/supersede/abort-signal).
- **Debug logging**: `PI_RC_DEBUG=1` (→ `~/.pi/agent/rc-debug.log`) or `PI_RC_DEBUG_FILE=<path>` — every lifecycle event + wire frame, append-only, off by default.
- **Tailscale bind**: PATH `tailscale` → `/Applications/Tailscale.app/Contents/MacOS/Tailscale` → fail loud (never 0.0.0.0). `PI_RC_BIND` overrides.
- **Node harness** `pi-extensions/rc/test-client.mjs` (~1350 lines): spawns pi in RPC mode in a temp copy of `test-project/`, 35 tests in groups 0–12 (rate-limit tests run LAST — group 11 — lockout would poison earlier groups; group 12 starts behind a `waitForLockoutClear`). Group 5 includes the ask() signal-path tests driven by `test-project/rc-signal-probe.ts` (test-only extension loaded into the child via `--extension`; the esc hatch's ctx.ui.custom can't run in the RPC spawn, so the signal/abort path is covered at the singleton level: already-aborted → null + cancelled broadcast; mid-wait abort → null + cancelled + late answer gets `unknown_question`). Group 12 = fake local APNs H2 endpoint tests: push send contract (settled/question/410-drop/persistence), the zero-client ask fallback, the locked-phone question push + redelivery, the --no-apns token-without-creds trap, the auto-auth hello test (k), and the uppercase-`push_token` registration → lowercase auto-auth test. The child's rc-push.json is isolated via `PI_RC_APNS_CONFIG` pointed at a (initially absent) temp file, so the user's real token/creds never leak into harness runs and the child starts with no APNs creds.
- **Swift E2E** `openclient-llm-test/Features/Code/CodeEndToEndTests.swift` (+ RcE2eServer, MockModelServer, PosixProcessHandle, EventCollector): real `CodeServerClient` vs real pi+extension; LLM = local deterministic `MockModelServer` (simulator test processes have no non-loopback egress); hermetic, 6 tests.
- **Streaming fixes** (commits `0ee914e`, `04a1580`, `91922e8`, `c0aa449` on `code-rc`), all in `openclient-llm/Shared/Features/Code/`:
  - User prompt local echo with dedup (dedup reuses the trailing identical user item and resets its `failed` flag).
  - `message_update` snapshot replace: content blocks pattern-matched from `AnyCodableValue`, NOT strict `CodeContentBlock` (raw wire thinking blocks key `thinking`; tool calls are `{toolCall,id,name,arguments}` — strict decoding would throw).
  - `message_start` role filter: only `role == "assistant"` frames open a bubble — **pi also emits `message_start` for the user prompt** (`pi-agent-core agent-loop.js:51`) and the RC server forwards it; the unfiltered client created a phantom empty bubble per prompt (root cause of the phone "double sparkle").
  - Tool steps: matched by `toolCallId` (not "last step"), marked complete on `tool_execution_end`; `updateToolOutput` reads `partialResult` (cumulative — REPLACE, pi sends response-so-far `agent-loop.js:219-223`) and `result` — the old `output` key was dead (live tool output never rendered, pre-existing bug).
  - Empty assistant bubbles dropped in `finalizeStreamingBubbles` (single cleanup point, `turn_end` + `agent_settled`); `agent_settled → send(.refreshState)` for the context header.
  - Failed-send handling: `CodeServerClient.send` returns success (was swallow-and-log); prompt/steer echoes marked failed on dead-socket send or server `not_idle` rejection; failed bubble shows a retry badge (a `Button`, not a tap gesture — selectable text eats taps), tap re-sends the same text reusing the item (bypasses the local `!isStreaming` pre-block — server is the authority).
  - **Retry hardening** (commit `2363be9`, after code-critic review): `not_idle` correlates by pending prompt-echo UUIDs, not position — ordered `pendingPromptEchoes` list in the VM (prompts only; steers are never rejected `not_idle`), newest entry consumed per rejection; entries clear on `agent_start`, history replacement, session rebind, disconnect. `handleRetryPrompt` guards `failed == true` (double-tap no longer double-sends). Mock records `attempts` vs `successes` separately (previously a failed attempt counted as sent). Known residual (accepted): if `agent_start` reaches the client before the `not_idle` frame for a raced second prompt, the pending list is already cleared → that prompt gets the toast but no failed badge.
- **Device-verified (real phone on tailnet, 2026-09-06):** paired with 6-digit code, prompted, streamed, keepalive pings; single assistant bubble per message (no phantom) and tool spinners complete; context header tracks real token usage after turns (the `agent_settled` refresh works end-to-end).
- **Code review** of the streaming fixes: verdict "Request Changes (minor)", all items fixed and re-verified against code + pi source (review doc `_code-review.md` deleted after the fixes landed — its durable conclusions are in this file and in Key invariants).

- **Status line** (commit `b6d5e76`): while serving, footer shows `rc: ws://<host>:<port> code <code> · <clients> · <push>` — client count (authenticated sockets) + push readiness (`push ok` = APNs creds resolve AND device token registered; `push: no token` = creds ok, phone hasn't registered; `push: not configured` = creds missing). Dynamic: `refreshStatus()` fires on connect/disconnect/`push_token`/APNs-410-drop/`/rc push-setup` write; the one-shot `/rc` notify stays the short form; `stop()` still clears the status. push-setup reaches `refreshStatus` via the globalThis structural interface (no import cycle). From `a3cf442`: a 4th footer segment, rendered only when known, shows the last APNs outcome for ANY push path (finished/question/test): `apns 200`, `apns 410 BadDeviceToken (token dropped)`, `apns disabled: <reason>`, `apns error: <detail, truncated 48+…>` — `handlePushOutcome` records every path via `recordPushOutcome` (state.lastPush + refreshStatus); 410 during a push still drops the exact token first. New `/rc push-test` subcommand fires one APNs push (fixed `RC push test` payload, `rc-test` collapse id, deliberately no timeSensitive — same delivery path as finished pushes) to the registered token and reports the outcome via TUI notify; 410 drops the token. NOTE: extensions load once per pi process — a running session shows the new format only after a pi restart (or new session), NOT after a `/rc` toggle.

## Decisions (do not revisit without a deliberate decision)
- Unknown history roles/content blocks are DROPPED (`case .unknown: break`); tests pin `count == 0`. Do not re-introduce fallback mappings (e.g. `unknown → .text("")`).
- Content blocks in `message_update` are pattern-matched, not strictly decoded (wire shape differs from the normalized history shape — see Done above).
- Steer prompts render as plain user messages — verified: `mapHistoryEntry` has no steer-specific branch; steers hit the `role === 'user'` path (`index.ts:698`).
- Cumulative-snapshot premise verified against pi source (`pi-ai` types the stream `partial` as the live response-so-far `AssistantMessage`): replace-per-frame is correct. The RC server's `appendAssistantDelta` buffer does NOT corroborate this — it only feeds hello/`session_start` `streaming_buffer` snapshot frames.
- Retry bypasses the local `isStreaming` guard on purpose; a rejected retry re-marks the same bubble failed.

## Known follow-ups (non-blocking)
- Answer/questionnaire sends still discard `send`'s `Bool` (pre-existing swallow): a failed answer leaves pi waiting on the question while the client already moved on. Fix needs a UX decision (re-show modal? toast + cancel server-side?) — not implemented deliberately.
- `getContextUsage()` may return null at settle → context header silently keeps the stale value (edge case; not observed on device).
- History only re-syncs on connect — mid-session desyncs (e.g. a prompt accepted server-side but whose echo was already marked failed by a stale `not_idle`) persist until reconnect.
- APNs push notifications: spec `docs/plans/rc-push-notifications-spec.md` (commits `ba07122`, plan-critic fixes `d87bb79`, JWT fix `f8b3f53`), implementation in flight. DONE: `/rc push-setup` command (`352f27e`: apns.ts config+key validation+HTTP2/TLS sender, push-setup.ts 3-prompt flow, debug.ts; env>file precedence, atomic 600 `rc-push.json`); server integration (`1734000`: `push_token` WS frame — 64-hex validation, last-wins, 410 drops only the exact rejected token — finished push on every `agent_settled` with constant `rc-finished` collapse id (2026-09-07 `35521d9`: ALL pushes now gated on `/rc` being enabled — finished push checks `isServing()`, question pushes get it via `askAvailable`; user decision: no push traffic when rc is off, main-agent-only still via the pushAllowed mode guard), question push (timeSensitive, `rc-question`) on remote-question creation; harness group 12 vs fake local APNs H2 endpoint with in-harness JWT signature verification, full harness 27/27). TRAP (Node 24): APNs sends must use `http2.connect(new URL('https://...'))` — the string-authority form throws at construction and a custom `createConnection` TLS socket fails with 'Protocol error'. Swift client DONE (`492cb95` on main, code-critic APPROVE-WITH-NITS): `pushToken` frame case + encoding (frozen `{type:push_token,token}`), `RemoteNotificationManager` singleton (in-memory token, observer seam, `requestAuthorization` with `.timeSensitive`), AppDelegate registration + delegate (suppress foreground `[]`, tap = no-op foreground), token sent on helloOk (connect + reconnect) and on update-while-connected, auth requested contextually on connect; `LocalNotificationManager` auth options also gained `.timeSensitive` (first-grant-only semantics — behavior-neutral). Critic MINORs closed in `cea98ac` (re-register on foreground after `didFailToRegister` via `pushRegistrationFailed` flag; `test_reconnect_helloOk_withToken_sendsPushTokenAgain` pins the reconnect branch). REMAINING: (1) device test — operator step DONE (`e77fca0`: Time Sensitive capability enabled on App ID, Xcode added `com.apple.developer.usernotifications.time-sensitive` to the entitlements, profile regenerated, built binary verified via codesign); phone retry pending after the safeJson crash fix. TRAP (landmine, fixed): `5c3c650`'s half-finished refactor deleted local `safeJson` from rc/index.ts without the import (`debug.ts` does not export it) — the first real phone connection crashed the server uncaught on every client message (fixed `7229933` by dropping the redundant wrappers; `debug.ts`'s dbgLog already stringifies internally). Lesson: `node pi-extensions/rc/test-client.mjs` MUST run after ANY change under `pi-extensions/rc/` — these runtime ReferenceErrors are invisible to builds and only surface on live WS traffic. `.p8` key already placed on this mac (under `~/.pi/agent/apns/`, chmod 600, verified P-256); Team/Key IDs are deliberately NOT recorded here or in the spec (user rule: no real credentials/IDs in memory or plans) — captured by `/rc push-setup` into `~/.pi/agent/rc-push.json`. TRAP: Node `crypto.sign` default for EC keys is DER (71 B, rejected by APNs) — must use `{ key, dsaEncoding: "ieee-p1363" }` for 64 B raw R||S. Settled decisions: fixed push bodies (no agent/user text in payloads), singleton-persistent device token (last wins, survives disconnect), tap-to-reopen always reconnects via lastConnect, token resent on arrival/refresh mid-session, constant `rc-finished` collapse-id, app entitlements needs `time-sensitive` key + profile regen, config via explicit `/rc push-setup` command (asks Team ID, Key ID, .p8 PATH, optional host — never key contents; env vars override). 2026-09-07 (commits e512dfb, 139755e): (a) the 400 BadDeviceToken root cause was the ENDPOINT, not the JWT — dev-profile (non-App-Store) tokens only work against `api.sandbox.push.apple.com`; production rejects them with 400 + body reason `BadDeviceToken`; sandbox accepts both sandbox AND production tokens, so sandbox is now the default host (env `PI_RC_APNS_HOST` > file `host` field > default; push-setup has an optional host prompt, empty = default; the JWT keeps full claims `{iss,sub,aud,iat,exp}` — a no-exp variant also got 200 on sandbox, but full claims are spec-compliant and were never the bug); (b) the device token persists in `rc-push.json` `token` (merge-preserved on push-setup rewrite; cleared on 410 for the exact token; restored at singleton init; the phone re-sends it every connect anyway — `/rc show-token` is the diagnostic); the config path itself is seam-able via `PI_RC_APNS_CONFIG` (mirrors `PI_RC_AUTH_FILE`) — the harness uses it to isolate the child; (c) AUTO-AUTH: `hello {version, code, token?}` authenticates on fresh-code match OR timing-safe match of `token` against the currently registered push token (bad token = failed hello like a bad code; first-time pairing still needs the code because registering one requires an authenticated connection). The app sends its own device token in hello (`CodeServerClient.hello(code:token:)`, optional wire field) — an already-paired device no longer needs re-pairing across pi sessions (the code is re-randomized per `/rc` toggle-on, which used to invalidate the Keychain-stored code every session).
- Locked-phone question gap: the `ask()` gate requires a live client, so the question push never fires while the phone is locked (spec: rc-push-notifications-spec.md, Open items). RESOLVED (9627cb3): the rc singleton's `askAvailable()` = serving AND (clients connected OR push-sendable [registered token AND resolvable APNs creds, resolved per call — never cached]); `ask()` and the question/questionnaire extensions gate on it, so a locked phone (0 clients) still routes remote, gets the timeSensitive push, and the pending question redelivers on unlock+reconnect. No sendable push → unchanged local TUI fallback. push-setup prompts deliberately keep the stricter `isServing() && hasConnectedClients()` gate. Pinned by group-12 tests (h)/(i)/(j); full harness green in both spawn modes (default 33 passed/0 failed/2 skipped, --no-apns 33/0/2, 2026-09-07). With the TUI esc hatch (see Remote ask item) a zero-client routed ask is no longer a dead end for the terminal user: Esc in the wait panel cancels the remote ask and the local TUI prompt runs.

## Not done
- Spec C4 manual matrix (most items covered by harness/E2E/phone smoke; notification/background/foreground/reconnect scenarios untested on device).
- 3 PRE-EXISTING test failures outside the Code feature (verified identical on clean HEAD via git stash): `ChatViewModelTests.test_send_sendTapped_withLongHistory_limitsRequestHistory` (50 vs 51), `LMStudioChatModelsTests.test_pluginIntegration_stringEncodingAndDecoding_roundTrips` + `test_request_withMCPIntegration_encodesNativeLMStudioFields`. Unrelated to RC; belongs to Chat/LMStudio work.

## Key invariants (don't regress)
- Protocol is FROZEN against the Swift client (`openclient-llm/Shared/Features/Code/Models/`): strict Codable decoding, so field names/shapes must match exactly. `contextUsage = {tokens, contextWindow, percent}`; `sessionId = sessionManager.getSessionId()` (NOT `getLeafId()`); error codes: `bad_code, rate_limited, version_mismatch, invalid_message, not_idle, unknown_question`.
- Question tool params carry no `value` — server-side `ask()` maps `value: label`.
- Singleton state lives on `globalThis[Symbol.for("pi-rc")]` (jiti loads extensions with `moduleCache:false`).
- The client must keep role-filtering `message_start` (user-role frames are transcript noise) and treat `message_update.message.content` as a full snapshot (replace, never append).
