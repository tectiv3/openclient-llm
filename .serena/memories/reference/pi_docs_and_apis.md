# pi docs + verified API pointers

- **pi docs** (local, read before touching pi integration): `(npx cache)/@earendil-works/pi-coding-agent/docs/` — `extensions.md` (extension API, `registerCommand`, ctx.ui, events), `rpc.md` (RPC mode JSONL protocol), `session-format.md`.
- **Verified pi API surface** (Task 0 probe, 2026-09-05) is captured in the spec's "Verified API surface" section — `docs/plans/rc-remote-control-spec.md`. Highlights: `pi.sendUserMessage(content, {deliverAs:"steer"|"followUp"})`, `ctx.abort()`, `ctx.isIdle()`, `ctx.getContextUsage()`, `ctx.shutdown()`, `ctx.sessionManager.getSessionId()` (stable Session UUID), `pi.getSessionName()`. RPC readiness = response to `get_session_stats`; extension commands dispatch via `{"type":"prompt","message":"/cmd"}` (response is only `{success:true}`).
- **Swift client = protocol contract**: `openclient-llm/Shared/Features/Code/Models/CodeModels.swift` + `CodeEvent.swift` (strict Codable — server must emit exactly these shapes) and `CodeServerClient.swift` (client API: `connect(host:port:code:) -> AsyncStream<CodeEvent>`, `send(CodeClientMessage)`).
- **Spec sections**: A = extension design (A5 remote ask, A7 rate limit), B = Swift feature, C = test design (C2 Node harness, C3.1 Swift E2E, C4 manual matrix), D = task workflow. Spec contains "Correction to the draft" notes where implementation deviated — read those, they override the original sketch.
